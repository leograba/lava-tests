# Your first LAVA JOB definition for a Toradex Colibri iMX6
device_type: colibri-imx6
job_name: colibri-imx6 pipeline, first job

timeouts:
  job:
    minutes: 15
  action:
    minutes: 5
  connection:
    minutes: 2
priority: medium
visibility: public

# context allows specific values to be overridden or included
#context:
  # tell the qemu template which architecture is being tested
  # the template uses that to ensure that qemu-system-x86_64 is executed.
#  arch: amd64

metadata:
  # please change these fields when modifying this job for your own tests.
  format: Lava-Test Test Definition 1.0
  name: colibri-imx6-firsttest
  description: First test written for the Colibri iMX6 modules
  version: 1.0
  devices:
    - colibri-imx6s
    - colibri-imx6dl
  os:
    - oe
  source: no-source-yet
  path: my/path/colibri-first-job.yaml
  build-readme: no-build-readme-yet
  build-console: no-build-console-yet
  build-log: no-build-log-yet

# ACTION_BLOCK
actions:
#- deploy:
#    timeout:
#      minutes: 5
#    to: tftp
#    images:
#      rootfs:
#        #need to understand this image_arg http://localhost:8000/static/docs/v2/explain_first_job.html#deploy-action-for-qemu
#        image_arg: -drive format=raw,file={rootfs}
#        url: http://developer1.toradex.com/files/toradex-dev/uploads/media/Colibri/Linux/Images/Colibri-iMX6_LXDE-Image_2.7b3-20170630.tar.bz2
#        compression: bz2
#    os: oe

# BOOT_BLOCK
- boot:
    timeout:
      minutes: 2
#    need to change method and media
    method: u-boot
#    commands: ramdisk
    type: master
#    need to check if this prompts make sense
    prompts: ["root@colibri-imx6:"]
    auto_login:
      login_prompt: "login:"
      username: root

#- test:
#    timeout:
#      minutes: 5
#    definitions:
#    - repository: git://git.linaro.org/qa/test-definitions.git
#      from: git
#      path: ubuntu/smoke-tests-basic.yaml
#      name: smoke-tests
#    - repository: https://git.linaro.org/lava-team/lava-functional-tests.git
#      from: git
#      path: lava-test-shell/single-node/singlenode03.yaml
#      name: singlenode-advanced

#- run:
#    steps:
#      - lava-test-case test-ls-command --shell ls /home
#      - lava-test-case test-ls-fail --shell ls /home/debian

- test:
    name: first-test
    timeout:
      minutes: 5
    definitions:
      run:
        steps:
          - lava-test-case test-ls-command --shell ls /home
          - lava-test-case test-ls-fail --shell ls /home/debian


